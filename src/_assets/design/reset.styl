*
*::before
*::after
  box-sizing inherit

html
  box-sizing border-box
  font-size 50%

body
  margin 0
  overflow-x hidden
  color $color
  font-size 2rem
  background-color $background-color

img
  vertical-align bottom
